# ===========================================================================
# Dev API Secrets
# ===========================================================================
# Additional secrets for `ag infra up --env dev`.
# OPENAI_API_KEY should be in .env (not here).
#
# Usage: Copy to infra/secrets/dev_api_secrets.yml
# ===========================================================================

# PARALLEL_API_KEY: "..."
# SLACK_BOT_TOKEN: "xoxb-..."
# SLACK_SIGNING_SECRET: "..."
# ANTHROPIC_API_KEY: "sk-ant-..."
# GOOGLE_API_KEY: "..."
