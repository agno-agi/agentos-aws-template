# ===========================================================================
# Production API Secrets
# ===========================================================================
# Uploaded to AWS Secrets Manager by `ag infra up --env prd`.
# ECS pulls these at runtime.
#
# Usage: Copy to infra/secrets/prd_api_secrets.yml
# ===========================================================================

OPENAI_API_KEY: "sk-..."

# JWT_VERIFICATION_KEY: |
#   -----BEGIN PUBLIC KEY-----
#   ...
#   -----END PUBLIC KEY-----

# PARALLEL_API_KEY: "..."
# SLACK_BOT_TOKEN: "xoxb-..."
# SLACK_SIGNING_SECRET: "..."
# ANTHROPIC_API_KEY: "sk-ant-..."
# GOOGLE_API_KEY: "..."
